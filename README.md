# API REST em Node.js com autenticação JWT

API REST em Node.js com autenticação JWT. Stack: Node.js, Express, JWT, MongoDB/PostgreSQL. Arquivos principais: server.js, routes/, controllers/, middleware/auth.js, models/.

## Setup
```
npm install
```